# Default values for test.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

appname: currencyweb

app:
  contour: "no_contour_assigned"

replicaCount: 2

image:
  repository: badman02/currencyweb
  tag: "no_tag_assigned"

service:
  type: ClusterIP
  port: 80
  localport: 80
